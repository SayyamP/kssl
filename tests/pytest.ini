[pytest]
testpaths = backend database extraction frontend/selenium
python_files = test_*.py
python_functions = test_*
addopts = -v -ra --timeout=300 --html=report/pytest.html --self-contained-html
markers =
    p0: must pass before a deploy
    write: mutates data; needs KSSL_TEST_ALLOW_WRITES=1
